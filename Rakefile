require 'squib'

task default: [:deck]
task all: [:deck, :backs, :marketing, :ladders]

task(:deck)      { load 'deck.rb' }
task(:backs)     { load 'backs.rb' }
task(:marketing) { load 'marketing.rb' }
task(:ladders)   { load 'src/ladders.rb' }
